<div class="container-fluid">
	<div class="row">
		<%= render "shared/sidebar" %>

		<main role="main" class="col-md-9 col-lg-10 py-4 px-3 p-lg-5">
      <div id="alert-message"></div>

      <div class="modal fade" id="list-modal" tabindex="-1" role="dialog" aria-labelledby="label1" aria-hidden="true"></div>

      <div class="error-message-area"></div>

      <%= form_for(@list, remote: true) do |f| %>
      <div class="form-row">
        <div class="col-md-5 input-group mb-4">
          <%= f.text_field :name, class: "form-control", placeholder: "New List", maxlength: 50 %>
          <div class="input-group-append">
            <%= button_tag type: 'submit', class: "btn btn-secondary", id: "register-button" do %>
              <%= icon("fas", "plus") %>
            <% end %>
          </div>
        </div>
      </div>
      <% end %>

      <div class="table-responsive">
      <table class="table table-hover text-nowrap">
        <thead class="thead-dark">
          <tr>
            <th scope="col" class="font-weight-normal">Name</th>
            <th scope="col" class="font-weight-normal">Line数</th>
            <th scope="col" class="font-weight-normal">作成日</th>
            <th scope="col"></th>
          </tr>
        </thead>
        <tbody id="list-list">
        <% @lists.each do |list| %>
          <%= render("list", list: list) %>
        <% end %>
        </tbody>
      </table>
      </div>
      <%= paginate @lists, theme: 'twitter-bootstrap-4',
                                    pagination_class: "pagination-sm justify-content-center",
                                    nav_class: "d-inline-b",
                                    window: 1 %>

		</main>
	</div>
</div>
